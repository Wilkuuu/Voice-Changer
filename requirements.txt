torch>=2.0.0
torchaudio>=2.0.0
numpy
soundfile
librosa
gradio>=4.0.0
huggingface_hub

# Natural voice conversion (branch natural-clone)
openvoice-cli

# Translation pipeline
faster-whisper
argostranslate
edge-tts

# AI segment polishing
anthropic>=0.20.0

# High-quality TTS with voice cloning (XTTS v2) — Python 3.12 compatible fork
coqui-tts>=0.24.0

# Chatterbox Multilingual TTS — beats ElevenLabs, native Polish, MIT license
# pip install chatterbox-tts
chatterbox-tts

# F5-TTS — flow-matching TTS, best naturalness, fine-tunable for custom voices
# Polish community checkpoint: huggingface.co/Gregniuki/F5-tts_English_German_Polish
# pip install f5-tts
f5-tts

# ── Tab 1 VC cloning quality ──────────────────────────────────────────────
# Reference preprocessing (denoise / LUFS / VAD / best-window selection)
noisereduce
pyloudnorm
silero-vad

# Speaker similarity scoring (best-of-N selection + UI label).
# Primary: SpeechBrain ECAPA-TDNN. Fallback: Resemblyzer.
speechbrain
resemblyzer

# SOTA zero-shot voice conversion — stronger identity transfer than OpenVoice
# If the PyPI build fails, try:
#     pip install git+https://github.com/Plachtaa/seed-vc
seed-vc

